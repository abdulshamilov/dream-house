# 🚀 AI IMPROVEMENTS - API DOCUMENTATION FOR FRONTEND

## 📝 Что было добавлено

### 1. АВТОМАТИЧЕСКОЕ РАСПОЗНАВАНИЕ РЕКОМЕНДАЦИЙ
AI теперь **сама** определяет когда нужны рекомендации квартир без явной команды.

### 2. 4-УРОВНЕВЫЙ УМНЫЙ ПОИСК
Если не найдет по строгим фильтрам → автоматически ищет мягче → потом по тексту → возвращает топ.

### 3. ГИБКИЕ РЕЖИМЫ
Даже в режиме `free` AI может рекомендовать квартиры если распознает запрос.

---

## 🔌 API ENDPOINTS (Не изменились)

```
POST /api/cards/ai/chat/
```

---

## 📤 ПРИМЕРЫ ЗАПРОСОВ И ОТВЕТОВ

### Пример 1: Автоматическая рекомендация (БЕЗ явного поиска)

**Request:**
```bash
POST /api/cards/ai/chat/
Content-Type: application/json

{
  "message": "Мне нужна квартира с 2 комнатами в центре Махачкалы до 3 млн рублей",
  "mode": "free"  # ← Даже режим "free"!
}
```

**Response:**
```json
{
  "success": true,
  "response": "Отлично! Вот несколько отличных вариантов для вас:\n\n1. 📍 Центральная, 2 комнаты, 2 млн - Отличный ремонт, новостройка\n2. 📍 Партизанская, 2 комнаты, 2.5 млн - Уютная квартира на первом этаже\n...",
  "referenced_cards": [
    {
      "id": 1,
      "title": "Двухкомнатная в центре",
      "address": "ул. Центральная, д. 10",
      "price": 2000000,
      "rooms": 2,
      "area": 65.5,
      "city": "Махачкала",
      "rating": 4.8,
      "description": "Отличная квартира..."
    },
    {
      "id": 5,
      "title": "Уютная двухкомнатная",
      "address": "ул. Партизанская, д. 15",
      "price": 2500000,
      "rooms": 2,
      "area": 58.0,
      "city": "Махачкала",
      "rating": 4.5,
      "description": "Квартира на первом этаже..."
    }
  ]
}
```

**Что происходит за кулисами:**
```
✅ AI распознает что это запрос на рекомендацию
🔍 Парсит параметры: rooms=2, price_max=3000000, city=Махачкала
✅ Level 1: Ищет со ВСЕМИ фильтрами → Находит 6 карточек
💬 Генерирует красивый ответ с описанием
```

---

### Пример 2: Явный запрос на рекомендацию

**Request:**
```bash
POST /api/cards/ai/chat/
Content-Type: application/json

{
  "message": "Рекомендуй мне квартиры в Каспийске",
  "mode": "search"
}
```

**Response:**
```json
{
  "success": true,
  "response": "Конечно! В Каспийске есть несколько отличных вариантов:\n\n1. 🏠 Квартира-студия, 1.2 млн руб...",
  "referenced_cards": [
    {
      "id": 12,
      "title": "Студия в Каспийске",
      "address": "пр. Мира, д. 20",
      "price": 1200000,
      "rooms": 1,
      "area": 35.0,
      "city": "Каспийск",
      "rating": 4.2
    },
    {
      "id": 18,
      "title": "Двушка у моря",
      "address": "ул. Набережная, д. 5",
      "price": 2800000,
      "rooms": 2,
      "area": 72.0,
      "city": "Каспийск",
      "rating": 4.9
    }
  ]
}
```

---

### Пример 3: Вопрос НЕ про недвижимость (fallback)

**Request:**
```bash
POST /api/cards/ai/chat/
Content-Type: application/json

{
  "message": "Как рассчитать ипотеку?",
  "mode": "search"
}
```

**Response:**
```json
{
  "success": true,
  "response": "Отличный вопрос! Ипотека рассчитывается по формуле:\n\n💰 Ежемесячный платеж = (Сумма кредита × Процентная ставка) / (Кол-во месяцев)\n\nНапример:\n- Кредит: 2 000 000 рублей\n- Ставка: 12% годовых\n- Срок: 20 лет\n\nРассчитанный платеж: примерно 24,000 рублей в месяц\n\n🏠 Если вам нужна помощь с подбором квартиры под вашу ипотеку, я с удовольствием помогу!",
  "referenced_cards": []  # ← Пусто, т.к. это общая информация
}
```

---

### Пример 4: Partial параметры (fallback на уровни)

**Request:**
```bash
POST /api/cards/ai/chat/
Content-Type: application/json

{
  "message": "Покажи квартиры до 2 млн"
}
```

**Response:**
```json
{
  "success": true,
  "response": "Вот доступные варианты до 2 млн рублей:\n\n1. 🎯 Экономный вариант...",
  "referenced_cards": [
    {
      "id": 25,
      "title": "Бюджетная однушка",
      "price": 1500000,
      "rooms": 1,
      "area": 40.0,
      "rating": 4.1
    },
    {
      "id": 28,
      "title": "Доступная двушка",
      "price": 1900000,
      "rooms": 2,
      "area": 55.0,
      "rating": 4.3
    }
  ]
}
```

**Логика:**
```
🔍 Парсит: price_max = 2000000
✅ Level 1: Ищет только по цене (others=None)
✅ Если нашло → возвращает
⚠️ Если нет → Level 2 и т.д.
```

---

### Пример 5: Ошибка (нет результатов)

**Request:**
```bash
POST /api/cards/ai/chat/
Content-Type: application/json

{
  "message": "Квартира 100 комнат до 500 рублей в центре Атлантиды"
}
```

**Response:**
```json
{
  "success": true,
  "response": "К сожалению, по вашему запросу нет точных совпадений 😞\n\nМожно ли уточнить:\n- Может быть, расширить бюджет?\n- Рассмотреть другой район?\n- Нужно ли столько комнат?\n\nЯ с удовольствием помогу найти идеальный вариант!",
  "referenced_cards": []
}
```

---

## 🎛️ ЛОГИКА РАБОТЫ

### Распознавание типа запроса

AI распознает рекомендации по этим фразам:
```
"рекомендуй", "рекомендация"
"предложи", "предложение"
"показа", "покажи", "показать"
"найди", "найти", "ищу"
"подбери", "подборка"
"какие квартиры"
"есть ли", "есть"
"подходит", "подходящие"
"лучше", "хорошие"
"похожие", "подобные"
"интересуюсь", "интересует"
"хочу", "хотим", "нужна", "нужны"
```

### 4 Уровня поиска

**Level 1:** Все фильтры (цена + комнаты + город + тип)
```python
# Находит точное совпадение
"квартира 2 комнаты до 3 млн в махачкале" 
→ ищет price<=3млн AND rooms=2 AND city=1
```

**Level 2:** Основные фильтры (город + комнаты + тип)
```python
# Убирает цену если нет результатов
→ ищет city=1 AND rooms=2
```

**Level 3:** Текстовый поиск
```python
# Ищет по словам в названии, описании, адресе
→ Полнотекстовый поиск без фильтров
```

**Level 4:** Топ рейтинговые
```python
# Если совсем ничего не нашлось
→ Возвращает лучшие 6 квартир по рейтингу
```

---

## 📊 ПАРСИНГ ПАРАМЕТРОВ

AI автоматически извлекает из текста:

| Параметр | Примеры из текста | Результат |
|----------|------------------|-----------|
| **Цена** | "до 3 млн", "от 2 до 4 млн" | `price_min`, `price_max` |
| **Комнаты** | "2-комнатная", "двухкомнатная", "3 комнаты" | `rooms` |
| **Город** | "в махачкале", "каспийск", "дербент" | `city` |
| **Тип** | "квартира", "дом", "студия", "офис" | `house_type` |

### Примеры парсинга:

```
"квартира 2 комнаты до 3 млн в махачкале"
→ rooms=2, price_max=3000000, city=1

"дом 4 комнаты от 5 млн в каспийске"
→ rooms=4, price_min=5000000, city=2, house_type='house'

"студия в центре до 1.5 млн"
→ price_max=1500000, house_type='studio'
```

---

## ⚙️ ПОДДЕРЖИВАЕМЫЕ РЕЖИМЫ

### Режим `search`
```json
{
  "message": "...",
  "mode": "search"  # ← Умный режим с поиском
}
```
- Автоматически распознает рекомендации
- Выполняет 4-уровневый поиск
- Если вопрос не про недвижимость → ответит на вопрос

### Режим `free`
```json
{
  "message": "...",
  "mode": "free"  # ← Обычный чат
}
```
- НО! Если распознает рекомендацию + про недвижимость → все равно ищет
- Иначе просто отвечает текстом

---

## 🔧 ПАРАМЕТРЫ ЗАПРОСА

```json
{
  "message": "текст вопроса или запроса",      // ОБЯЗАТЕЛЬНО
  "mode": "search",                              // опционально, default='search'
  "user_preferences": {                          // опционально
    "price_min": 1000000,
    "price_max": 5000000,
    "rooms": 2,
    "city": 1,
    "house_type": "apartment"
  }
}
```

---

## 📋 ПРИМЕРЫ ДЛЯ ФРОНТА

### 1. Simple chat
```javascript
// Простой чат без явного запроса поиска
const response = await fetch('/api/cards/ai/chat/', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    message: "Рекомендуй квартиры в центре"
  })
});

const data = await response.json();
console.log(data.response);  // Красивый ответ с описанием
console.log(data.referenced_cards);  // [Card, Card, ...]
```

### 2. С явным режимом
```javascript
const response = await fetch('/api/cards/ai/chat/', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    message: "Квартира 2 комнаты до 3 млн",
    mode: 'search'
  })
});
```

### 3. С пользовательскими предпочтениями
```javascript
const response = await fetch('/api/cards/ai/chat/', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    message: "Какие варианты доступны?",
    user_preferences: {
      city: 1,           // Махачкала
      price_max: 3000000,
      rooms: 2
    }
  })
});
```

---

## 🎯 ГЛАВНОЕ УЛУЧШЕНИЕ

**ДО:**
```
Юзер: "Рекомендуй квартиры"
AI: "Что ты имеешь в виду? Используй команду /search"
```

**ПОСЛЕ:**
```
Юзер: "Рекомендуй квартиры"
AI: "✅ Вот 5-6 лучших вариантов:"
    [Красивые карточки квартир]
```

---

## 🚀 ЧТО РАБОТАЕТ СЕЙЧАС

✅ Автоматическое распознавание рекомендаций
✅ 4-уровневый fallback поиск
✅ Парсинг цены, комнат, города, типа
✅ Гибкие режимы (search + free)
✅ Красивые ответы с описанием
✅ Максимум 6 квартир в ответе
✅ Логирование с эмодзи

---

## 📞 ПОДДЕРЖКА

Если фронт сломалось или вопросы:
- Все методы в `cards/ai_service.py`
- Логирование в консоли с эмодзи
- Endpoint остался тот же: `/api/cards/ai/chat/`
